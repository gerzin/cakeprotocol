load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "main",
    srcs = [
        "application.cpp",
        "application.hpp",
        "main.cpp",
    ],
    deps = [
        "//src/screen_locker",
        "@spdlog",
    ],
)
